# 执行指令：独立实现异步全差分 SAR ADC 行为级模型

立即停止参考或拟合以下内容：

```text
任何 Verilog-A 实现
任何既有 SAR_LOGIC 文件
旧 Python SARADC 输出
旧 decisions 序列
历史 SNDR 数值
CodebookNearestSearch
WeightedGreedySAR
target_dac 正向累加模型
```

本任务不是复现某份已有代码，也不是对齐某个历史输出。

本任务的唯一目标是：

> 从开关电容 SAR ADC 的物理工作原理出发，独立实现一个完整、自洽、可运行的异步全差分 SAR ADC 行为级模型。

该模型必须真实表达：

```text
输入采样
底板开关状态
CDAC 电荷重分配
P/N 顶板电压
比较器完成事件
异步 SAR 控制握手
trial / compare / commit
转换完成
数字解码
```

不得用等效权重贪心累加代替实际电容切换。

---

## 一、模型边界

实现一个通用的异步全差分 SAR ADC：

```text
差分输入：VINP、VINN
差分 CDAC：P-side、N-side
参考电压：VREFP、VREFN
共模电压：VCM
动态比较器
异步 SAR 控制器
14 个数字转换阶段
13 个物理电容转换阶段 + 1 个数字 terminal 阶段
```

本模型需要物理自洽，但不声称逐行等价于任何外部 VA 文件。

所有行为由本项目内明确写出的：

```text
拓扑定义
开关规则
比较器极性
异步握手协议
时序参数
terminal 规则
```

共同决定。

---

## 二、旧模型全部降级为研究参考

当前基于：

```python
target_dac
accumulated
trial_weight
residue = target_dac - accumulated
```

的模型重命名为：

```python
WeightedGreedySARReference
```

移动到：

```text
python_cal/research/weighted_greedy_reference.py
```

排序码本模型继续位于：

```text
python_cal/research/codebook_search.py
```

这两个模型不得：

```text
作为默认 SARADC
参与实际异步转换
决定开关状态
作为性能 Gate
影响新模型的符号和极性
```

新核心模型命名为：

```python
AsyncBehavioralSARADC
```

---

## 三、项目结构

```text
python_cal/
├── topology/
│   ├── capacitor.py
│   ├── cdac_topology.py
│   ├── switch_state.py
│   └── switching_policy.py
├── physical/
│   ├── charge_state.py
│   ├── charge_solver.py
│   └── differential_cdac.py
├── comparator/
│   ├── dynamic_comparator.py
│   └── comparator_result.py
├── async_control/
│   ├── events.py
│   ├── timing.py
│   ├── sar_fsm.py
│   └── handshake.py
├── conversion/
│   ├── async_sar_adc.py
│   ├── conversion_result.py
│   └── trace.py
├── decode/
│   └── sar_decoder.py
└── tests/
    ├── test_charge_conservation.py
    ├── test_switching_policy.py
    ├── test_comparator.py
    ├── test_async_handshake.py
    ├── test_async_conversion.py
    └── test_decoder_isolation.py
```

---

## 四、物理电容必须显式建模

定义物理电容：

```python
@dataclass(frozen=True)
class Capacitor:
    name: str
    capacitance_f: float
    top_node: str
    bottom_node: str
    stage: int | None
```

每侧 CDAC 至少包含：

```text
低段：
L1C
L2C
L4C
L8C
L16C
L32C

桥接：
CB

高段：
H1C-A
H1C-R
H2C
H4C
H8C
H16C
H32C
```

差分网络包含：

```text
P-side 顶板节点
P-side bridge 节点
N-side 顶板节点
N-side bridge 节点
```

terminal 不得伪装成物理电容：

```python
is_physical = False
```

---

## 五、底板开关状态必须显式保存

底板允许连接：

```python
from enum import Enum

class Rail(Enum):
    VREFP = "vrefp"
    VREFN = "vrefn"
    VCM = "vcm"
    VINP = "vinp"
    VINN = "vinn"
    FLOAT = "float"
```

每个物理电容必须拥有独立开关状态。

```python
@dataclass(frozen=True)
class SideSwitchState:
    low_1c: Rail
    low_2c: Rail
    low_4c: Rail
    low_8c: Rail
    low_16c: Rail
    low_32c: Rail

    high_1c_a: Rail
    high_1c_r: Rail
    high_2c: Rail
    high_4c: Rail
    high_8c: Rail
    high_16c: Rail
    high_32c: Rail
```

完整差分状态：

```python
@dataclass(frozen=True)
class DifferentialSwitchState:
    p_side: SideSwitchState
    n_side: SideSwitchState
```

禁止只保存：

```python
decisions = [0, 1, ...]
```

`decisions` 只能由已提交的实际开关动作派生。

---

## 六、必须真实模拟采样阶段

采样阶段必须完成以下工作：

```text
连接 VINP、VINN、VCM
设置 P/N 两侧所有底板状态
建立顶板和 bridge 节点的初始电荷
断开采样开关
使顶板节点进入浮动保持状态
```

定义：

```python
@dataclass(frozen=True)
class SampledChargeState:
    p_top_charge_c: float
    p_bridge_charge_c: float
    n_top_charge_c: float
    n_bridge_charge_c: float
    sampled_switch_state: DifferentialSwitchState
```

转换阶段不得直接读取 `vin_diff` 并计算目标 DAC 数值。

输入只能通过采样阶段产生的初始电荷影响转换。

---

## 七、使用节点电荷守恒求解

每次底板开关改变后，都必须重新求解浮动节点电压。

对于每个浮动节点：

[
Q_{\text{node}}
===============

\sum_i C_i(V_{\text{node}}-V_{\text{bottom},i})
]

采样结束后，节点保存的总电荷不变。

将网络写成：

[
A\mathbf{v}=\mathbf{b}
]

求解至少四个未知节点：

```text
VTOP_P
VBRIDGE_P
VTOP_N
VBRIDGE_N
```

接口：

```python
@dataclass(frozen=True)
class CDACNodeSolution:
    vtop_p: float
    vbridge_p: float
    vtop_n: float
    vbridge_n: float

    @property
    def differential_input(self) -> float:
        return self.vtop_p - self.vtop_n
```

求解器：

```python
class DifferentialChargeSolver:
    def solve(
        self,
        sampled_charge: SampledChargeState,
        switch_state: DifferentialSwitchState,
    ) -> CDACNodeSolution:
        ...
```

禁止在求解器中调用：

```text
effective_weights
target_dac
subset sum
nearest level
理想 ADC code
```

---

## 八、实现明确的开关策略

实现一个独立的开关策略对象：

```python
class DifferentialSwitchingPolicy:
    def sampling_state(self) -> DifferentialSwitchState:
        ...

    def reset_state(self) -> DifferentialSwitchState:
        ...

    def trial_state(
        self,
        stage: int,
        committed_state: DifferentialSwitchState,
    ) -> DifferentialSwitchState:
        ...

    def commit_state(
        self,
        stage: int,
        committed_state: DifferentialSwitchState,
        trial_state: DifferentialSwitchState,
        comparator_result: int,
    ) -> DifferentialSwitchState:
        ...
```

开关策略必须明确写清楚：

```text
P-side 当前 stage 接 VREFP 还是 VREFN
N-side 当前 stage 接 VREFN 还是 VREFP
trial 状态如何产生
比较结果为 1 时保留哪个状态
比较结果为 0 时切换到哪个状态
H1C-R 的角色
低段各电容的顺序
terminal 如何生成最后数字判决
```

这些规则必须在本项目中成为显式设计规范。

不得通过跑 SNDR 后反复修改极性来拟合结果。

---

## 九、比较器是异步事件源

实现动态比较器：

```python
@dataclass(frozen=True)
class ComparatorResult:
    output: int
    differential_v: float
    decision_time_s: float
    metastable: bool
```

接口：

```python
class DynamicComparator:
    def request(
        self,
        vtop_p: float,
        vtop_n: float,
        request_time_s: float,
        rng=None,
    ) -> ComparatorResult:
        ...
```

至少支持：

```text
输入失调
输入等效噪声
有限决策时间
差分越小，决策越慢
metastability threshold
最大等待时间
```

建议决策时间模型：

[
t_{\mathrm{decision}}
=====================

t_0+
\tau\ln\left(
\frac{V_{\mathrm{logic}}}
{\max(|V_{\mathrm{diff}}+V_{\mathrm{offset}}+V_{\mathrm{noise}}|,V_{\min})}
\right)
]

该表达式可以作为行为级近似，但参数必须配置化。

比较器输出极性必须固定并写入文档：

```text
output = 1 ⇔ VTOP_P > VTOP_N
```

或者相反。

只能选择一种，之后不得根据性能随意更改。

---

## 十、实现真正异步 SAR 控制

不得使用固定周期：

```text
for stage in range(14):
    compare()
```

作为时序模型。

转换必须由事件驱动：

```text
SAMPLE_REQ
SAMPLE_DONE
CONVERT_REQ
DAC_TRIAL_APPLIED
DAC_SETTLED
CMP_REQ
CMP_DONE
BIT_COMMITTED
NEXT_STAGE
CONVERSION_DONE
RESET
```

定义事件：

```python
class AsyncEventType(Enum):
    SAMPLE_START = "sample_start"
    SAMPLE_DONE = "sample_done"
    DAC_TRIAL_START = "dac_trial_start"
    DAC_SETTLED = "dac_settled"
    CMP_REQUEST = "cmp_request"
    CMP_DONE = "cmp_done"
    BIT_COMMIT = "bit_commit"
    CONVERSION_DONE = "conversion_done"
    RESET_DONE = "reset_done"
```

事件：

```python
@dataclass(frozen=True)
class AsyncEvent:
    time_s: float
    event_type: AsyncEventType
    stage: int | None
```

异步控制器必须等待：

```text
DAC settled
比较器完成
commit 完成
```

才能进入下一阶段。

下一 bit 的开始时间必须由上一 bit 的完成事件决定，不得由全局同步时钟固定推进。

---

## 十一、异步状态机

实现状态：

```python
class SARState(Enum):
    IDLE = "idle"
    SAMPLING = "sampling"
    HOLD = "hold"
    APPLY_TRIAL = "apply_trial"
    WAIT_DAC = "wait_dac"
    REQUEST_COMPARE = "request_compare"
    WAIT_COMPARE = "wait_compare"
    COMMIT_BIT = "commit_bit"
    NEXT_BIT = "next_bit"
    DONE = "done"
    RESET = "reset"
```

主控制器：

```python
class AsyncSARController:
    def start_conversion(
        self,
        vinp: float,
        vinn: float,
        start_time_s: float,
        rng=None,
    ):
        ...
```

每阶段流程：

```text
1. 读取 committed switch state
2. 生成 trial switch state
3. 应用底板切换
4. 等待 DAC settling
5. 求解 trial 后节点电压
6. 发起 comparator request
7. 等待 comparator done
8. 根据比较结果提交开关
9. 保存 bit decision
10. 产生下一 stage 事件
```

---

## 十二、DAC settling 必须有行为模型

开关切换后不得假设瞬时稳定。

实现：

```python
@dataclass(frozen=True)
class DACSettlingResult:
    settled_solution: CDACNodeSolution
    settle_time_s: float
    residual_error_v: float
```

可以支持两种模式：

```text
ideal_settling=True：
    直接到达电荷守恒稳态

ideal_settling=False：
    根据 RC 时间常数计算有限 settling error
```

例如：

[
V(t)=V_\infty+(V_0-V_\infty)e^{-t/\tau}
]

异步控制器只有收到 `DAC_SETTLED` 后才能请求比较器。

---

## 十三、完整转换 trace

每个转换必须产生事件级 trace：

```python
@dataclass(frozen=True)
class AsyncSARStepTrace:
    stage: int
    stage_name: str

    trial_start_time_s: float
    dac_settled_time_s: float
    comparator_done_time_s: float
    commit_time_s: float

    committed_before: DifferentialSwitchState
    trial_state: DifferentialSwitchState
    committed_after: DifferentialSwitchState

    vtop_p: float
    vtop_n: float
    vbridge_p: float
    vbridge_n: float
    differential_v: float

    comparator_output: int
    comparator_metastable: bool
    decision: int
```

完整结果：

```python
@dataclass(frozen=True)
class AsyncSARConversionResult:
    sampled_charge: SampledChargeState
    final_switch_state: DifferentialSwitchState
    decisions: tuple[int, ...]
    steps: tuple[AsyncSARStepTrace, ...]
    events: tuple[AsyncEvent, ...]
    start_time_s: float
    done_time_s: float
```

---

## 十四、数字解码与物理转换严格分离

物理 ADC：

```python
conversion = adc.convert(vinp, vinn, rng)
```

只返回：

```text
开关状态
比较器结果
decision register
事件 trace
转换时间
```

数字解码：

```python
decode_result = decoder.decode_one_detailed(conversion.decisions)
```

解码权重更新不得影响：

```text
采样
电荷
顶板电压
trial state
比较器
异步事件时间
物理 decisions
```

必须有测试：

```python
before = adc.convert(vinp, vinn)
decoder.update_weights(new_weights)
after = adc.convert(vinp, vinn)

assert before.decisions == after.decisions
assert before.events == after.events
```

在无噪声确定性模式下，两者必须完全一致。

---

## 十五、必须实施的测试

### 1. 电荷守恒

```text
采样后浮动节点电荷保持
任何底板切换后节点总电荷不变
P/N 对称电路满足对称性
```

### 2. 单电容开关

依次切换每个电容，验证：

```text
顶板电压变化方向
桥接节点变化方向
差分输出方向
P/N 互换后的反对称关系
```

### 3. 共模输入

当：

```text
VINP = VINN
```

理想对称模型应满足：

```text
VTOP_P - VTOP_N ≈ 0
```

### 4. 输入反相

对于：

```text
VINP - VINN = +x
VINP - VINN = -x
```

决策结果应满足差分对称关系。

### 5. 异步握手顺序

每一级必须满足：

```text
trial_start
<
dac_settled
<
cmp_done
<
commit
<
next_stage_trial_start
```

禁止事件倒序或重叠。

### 6. 转换时间依赖输入

比较器差分较小时：

```text
decision_time 更长
```

因此完整转换时间不应永远固定。

这是真正异步模型的重要验证。

### 7. metastability

输入接近决策边界时，验证：

```text
metastable flag
timeout
fallback policy
```

行为一致。

### 8. Decoder isolation

改变 decoder weights 不得改变物理 decisions。

---

## 十六、不得做的事情

严禁：

```text
参考 VA decisions
对照 VA trace
拟合历史 SNDR
根据输出性能修改开关极性
使用 target_dac
使用 accumulated weight
使用 subset-sum
使用 nearest-level
使用全码本
提前知道理想 code
从理想 code 反推 decisions
用 effective weights 替代电荷求解
固定 14 拍同步时钟推进
每一级无条件固定延时
把 comparator 当成普通符号函数而没有完成事件
```

---

## 十七、实施顺序

直接完成整个异步行为模型，不再做临时 greedy 或 signed-weight 过渡模型。

```text
1. 定义物理电容拓扑
2. 定义所有底板开关状态
3. 实现采样和初始电荷
4. 实现 P/N 电荷守恒求解器
5. 定义差分开关策略
6. 实现 DAC settling
7. 实现动态比较器及决策时间
8. 实现异步事件和握手
9. 实现 SAR trial / compare / commit
10. 实现完整事件 trace
11. 接入独立 decoder
12. 完成守恒、对称、时序和隔离测试
13. 最后才运行静态 transfer curve 和 FFT
```

---

## 十八、完成标准

只有满足以下全部条件，才算完成：

```text
输入通过真实采样开关进入电容网络
所有物理电容底板状态均可观察
所有转换动作都是实际开关切换
顶板电压由电荷守恒求解
桥接节点参与实际求解
比较器具有 request/done 异步行为
每 bit 由上一 bit 完成事件触发
转换时间随比较器决策时间变化
每一级具有 trial 和 commit 状态
decoder 不影响物理 decisions
没有 target_dac、贪心累加或码本搜索
```

优先交付：

```text
一个完整异步转换的事件时间线
14-stage 开关状态
每一级 P/N 顶板电压
每一级比较器完成时间
最终 decisions
总转换时间
```

不要对照任何外部实现，不要用任何历史 SNDR 指导模型。先确保模型由电路拓扑、电荷守恒和异步握手规则自洽地产生结果。
