# 复审综合结论 40：ChatGPT 复审与本审查方独立核查的合并仲裁

**文档性质：** 合并外部复审（ChatGPT《复审结论/当前进度评估》）与本审查方 38/39 号文档及后续独立实验，对全部判定逐项仲裁，形成统一工程状态与行动清单。

**编号说明：** 39 号为本审查方《监控结论》（已递交）；本文档为 40；《完整复现方案》顺延至 41。

**仲裁依据（本审查方独有、ChatGPT 不具备的证据）：**
- 直接读取**当前生产源码**（sar_decoder.py @19:23 含 ÷2 公式；calibration_controller.py @19:33 含 terminal 判定步）；
- 通过 import 生产包独立重跑：全量程 FFT **73.58 dB**、DC **0/4096 缺码**、**零噪声 ideal 校准 7/7 target 精确命中物理值**（65/65/130/260/520/1040/2080）、测试 **39/39 通过**（`reports/review_monitor_check.py`）；
- 对构建方 dc_sweep_raw.csv / reachable_levels.csv 的独立统计。

---

## 一、仲裁总表（ChatGPT 判定 vs 本审查方证据 → 最终判定）

| 议题 | ChatGPT 判定 | 本审查方证据 | 最终仲裁 |
| --- | --- | --- | --- |
| 67.5 dB 根因 = decoder ÷2 + VFS 二倍 | PASS（方向正确） | 一致确认（39 号 §2.1，数字严丝合缝） | **成立** |
| 修正后理想桥接 SAR = 12-bit（73.59 / 0 缺码） | PASS | 独立复现 73.58 / 0 缺码（生产代码） | **成立** |
| Decoder 是否合入生产源码 | **UNVERIFIED** | 生产 sar_decoder.py 含 ÷2；本审查方全部结果均 import 生产包获得；39/39 测试过 | **升级为 VERIFIED（源码级）**；git commit 证据仍缺（python_cal 未纳入 git 跟踪，见 §五 R6） |
| 校准 | **FAIL / NOT REVALIDATED** | 当前生产代码零噪声 ideal 校准 7/7 **精确命中**（65/65/130/260/520/1040/2080，误差 0） | **分层：测量精度 FIXED（独立确认）；校准有效性（SNDR 归因）NOT REVALIDATED（等 R1）** |
| 证据包质量 | 多处不一致 | 一致 + 各有新发现（§三/§四） | D1–D6 待修复 |
| codebook 实验 | 仍然错误，结论撤回 | 一致（39 号 D4）；采纳其残差优势判据（§三.3） | 待修 |
| CB sweep 缺码列 | 无效（1025 点稀疏扫描所致，4096−1025=3071） | 采纳——比本审查方 39 号 D2 更深一层 | **确认无效，撤销该列全部数字** |
| 总体 | NOT READY | 一致 | **NOT READY（但两个单元格升级，见 §六）** |

---

## 二、本审查方证据带来的两项升级

### 2.1 Decoder 生产集成：UNVERIFIED → VERIFIED（源码级）

ChatGPT 质疑"修正是否只在 forensic 脚本中临时使用"。本审查方直接确认：

1. **生产源码** `python_cal/decode/sar_decoder.py` 现行公式为 `code = (signed_sum + signal_weight) · max_code / (2 · signal_weight)`（÷2），非临时脚本；
2. 本审查方的 73.58 dB / 0 缺码 / 校准精确命中全部通过 `import python_cal`（生产包）获得，与 forensic 脚本无关；
3. 测试套件 39/39 通过，其中 3 项新增测试直接锁定该行为（全 2^14 组合 decoder 覆盖 4096 码、physical+terminal 覆盖全部 signed level、signed_sum 恒奇）。
4. 残留缺口：**git 层面 python_cal 整个目录未被跟踪**（git status 显示 `?? ./`），"commit = 1f913b8f" 对该目录无约束力。要求将 python_cal 纳入版本管理并给出新 commit hash（R6）。

### 2.2 校准：FAIL → 分层判定

ChatGPT 的 "Calibration: FAIL" 依据的是 v2.0.0 旧报告（64/65 链），未见当前代码。本审查方在当前生产代码上实测：

```text
零噪声 ideal 校准（avg_pairs=16, cal_noise_sigma=0.0）:
  H1C-A 65.0 / H1C-R 65.0 / H2C 130.0 / H4C 260.0
  H8C 520.0 / H16C 1040.0 / H32C 2080.0
  = 物理真值，误差 0（terminal ±1 Q0 判定步生效）
```

因此：**权重测量精度 = FIXED（独立确认）**；但**校准有效性 = NOT REVALIDATED**——7 场景 + MC 的 nominal/calibrated/physical SNDR 归因表必须在新 decoder + terminal 校准下重生成（R1），通过判据沿用 38 号 §五并吸收 ChatGPT 的 P0：

```text
ideal calibrated SNDR ≥ ideal nominal SNDR − 0.1 dB
无任何场景 cal gain < −1 dB；大失配场景保持显著正增益
```

遗留备注（39 号已记）：`terminal_adj` 目前用求解器无噪声符号判定，`terminal_cmp_result` 仅入 trace 未参与判定；含噪运行时该设计略优于真实硅片。建议改为使用比较器输出并靠对平均收敛，或在文档显式声明此简化。

---

## 三、ChatGPT 的新增发现（本审查方核实并采纳）

1. **CB sweep 缺码列无效的根因**：3071 = 4096 − 1025 → 扫描仅 ~1025 个输入点，把"未观察到"当"缺失"。本审查方 39 号 D2 只指出矛盾，未定位到此。采纳：**撤销 CB sweep 全部 missing_codes 数字**；重做需 ≥65537 点（~16 samples/code）或 transition 二分搜索，字段拆分 `input_points / observed_unique_codes / unobserved_due_to_sampling / proven_missing_codes`（新增 D6）。
2. **reachable_levels 缺码数是 63 不是 65**：本审查方独立统计 unique_code_nominal = 4033 → 缺 63 ✓，报告 JSON 写 65 有误。同时采纳其命名建议：`reachable_physical_states_without_terminal.csv`（2^13 状态）与 `reachable_final_codes_with_terminal.csv`（14 判决）必须分开——terminal 补齐的正是这 63 个码，两组数据本身不矛盾，但文件与报告必须区分。
3. **codebook 残差优势判据**：采纳为硬性验收——对全部 FFT 输入逐样本断言 `|codebook_residual| ≤ |fixed_sar_residual| + 1e-12`；若残差更小而 code 更差，则 codebook 的 decision→decoder 映射错误。本审查方 39 号 D4（target 归一化 `vin/(2·VREF)+0.5` 与真实满量程不符）是其嫌疑点之一。
4. **两处措辞修正**：删除 "2-LSB bottom step / 68 dB(11-bit) 上限"（与自家 73.59/0 缺码直接冲突，39 号 D3）；"parity lock 是真实电路缺陷"改写为"整数 Q0 + 正确 ÷2 下不存在系统性缺码；失配引起的是 transition level 偏移（DNL/INL），与旧 decoder 的 50% 奇偶缺码不是同一问题"。

---

## 四、ChatGPT 报告中的错误/过时之处（以本审查方证据纠正）

1. **"Calibration: FAIL"** —— 基于 stale v2.0.0 报告。当前生产代码测量精度已修复（§2.2）。正确表述：**测量 FIXED，有效性 NOT REVALIDATED**。
2. **"缺少真正生成新报告的脚本"** —— `run_bridge_forensic.py`（52 KB，19:36）就在包内，即生成器。需验证"一条命令全量重生成"（本审查方已在隔离副本中启动验证，结果见本文档补记 §七）。
3. **"review38_experiments.py 仍使用旧满量程，不可能生成 73.59"** —— 对象错误：那是**本审查方**在修复前写的核查脚本（记录了老 decoder + 半量程的混合态现象），不是报告生成器；已标记 OBSOLETE 头注，当前核查用 `review_monitor_check.py`。
4. **频谱 CSV 字节级相同** —— ChatGPT 重算 73.59 后接受其可信度；本审查方维持 D1 缺陷判定：**交付物必须分别独立导出**，哪怕结论已被他证确认——证据循环在审查链中不可接受。
5. **测试状态** —— ChatGPT 未见测试结果；实际 39/39 通过。

---

## 五、合并行动清单（去重、排序、带验收判据）

| # | 行动 | 验收判据 | 来源 |
| --- | --- | --- | --- |
| R1 | **重生成 behavioral_sar_run_report**（新 decoder + terminal 校准 + VFS=0.8599），7 场景 + MC 归因表 | VFS≈0.8599；ideal nominal/physical/calibrated ≈73.5±0.2；ideal cal gain ≥ −0.1 dB；无场景 < −1 dB；缺码 0；旧报告删除或标记 OBSOLETE | 双方 P0 |
| R2 | FFT 频谱 CSV 独立重导（D1） | 两文件 diff 必有差异；bridge SAR 频谱与重算 SNDR 一致 | 39 |
| R3 | codebook 修复（归一化 + terminal + 映射）并重测 | 逐样本残差优势断言通过；codebook SNDR ≥ fixed SAR − 0.5 dB 方可谈 near-optimal | 双方 |
| R4 | CB sweep 重做（D6）+ 删除 68 dB/11-bit 段落与 parity-lock 措辞（D3） | ≥65537 点或 transition 搜索；四字段拆分 | 双方 |
| R5 | VA decoder 逐行对账（新 SARDecoder vs VA 802–829 行；decode_va_exact 互补极性 code_va = 4095 − code_sar 写入文档） | 一句结论：真片 decoder 是否含 ÷2 归一化 | 39（涉硅片，最优先） |
| R6 | python_cal 纳入 git 跟踪并给新 commit hash | git log 含本次修复 | ChatGPT |
| R7 | reachable_levels 文件拆分命名（with/without terminal）+ JSON 63/65 更正 | 报告与 CSV 数字一致 | ChatGPT（本审查方核实 63 ✓） |

---

## 六、当前工程状态（五阶段框架，单元格已按本审查方证据更新）

| 阶段 | 状态 |
| --- | --- |
| 1. 真实开关 + 电荷守恒网络 | **完成** |
| 2. 异步 SAR trial/compare/commit | **完成**（trace 56 阶段符号一致性 0 失配，双方各自验证） |
| 3. 正确 decoder + full-scale + ideal FFT | **完成**（生产源码级验证；git provenance 待 R6） |
| 4. Huang D+/D− 校准闭环 | **测量精度完成（7/7 精确）；有效性待 R1 重审** |
| 5. mismatch / MC / PVT / settling / DNL-INL sign-off | **未完成**（38 号 P1 清单继续有效） |

**最终判定：**

```text
Ideal SAR conversion:              PASS（双方独立确认）
Bridge architecture diagnosis:     PASS
Decoder production integration:    VERIFIED（源码级；git commit 待 R6）
Calibration measurement:           FIXED（7/7 精确，独立确认）
Calibration effectiveness:         NOT REVALIDATED（等 R1）
Evidence package:                  D1–D6 待修复
Overall project:                   NOT READY
```

---

## 七、补记：run_bridge_forensic.py 一条命令可复现性验证（已完成）

本审查方在隔离副本 `$TEMP/python_cal_audit` 中执行 `python python_cal/run_bridge_forensic.py`（PYTHONPATH 指向副本，不触碰证据原件）：

```text
运行 57.9 s，正常结束，Verdict: MODELING_BUG
全部交付物重新生成：bridge_root_cause_report.md/json、dc_sweep_raw.csv、
  fft_spectrum_ideal_quantizer.csv、fft_spectrum_bridge_sar.csv、
  reachable_levels.csv、single_cap_steps.csv、conversion_trace_*.csv、test_output.txt
与已交付原件逐字节比对：全部 IDENTICAL（确定性可复现，种子固定）
```

**结论：ChatGPT 的"缺少生成脚本 / 一条命令可复现性"P0 不成立——生成器存在且可独立复现。** 但复跑同时确认：**生成器写出的两个 FFT 频谱 CSV 依然字节级相同**（D1 是生成器自身的 bug，不是一次性拷贝失误），修复责任在 `run_bridge_forensic.py` 的频谱导出段。

附：复跑日志中 CB sweep 的 missing 数字（3071/3081）与原件一致，再次确认 §三.1 的稀疏扫描根因（非随机抖动）。
