# 监控结论 39：对《bridge_root_cause_report》的独立核查

**核查对象：** `reports/bridge_root_cause_report.md/.json`（Verdict: MODELING_BUG）及全部附件 CSV、`run_bridge_forensic.py`、三处源码修改（sar_decoder / calibration_controller / handshake）、新增 3 项测试。

**核查方式（全部独立执行，未修改任何模型源码）：**
1. 从构建方 `dc_sweep_raw.csv` / `reachable_levels.csv` 原始数据独立统计缺码、单调性、奇偶模式；
2. 用当前代码（新 decoder）独立重跑 FFT 与 DC 扫描（半量程 ±0.4301 V 与全量程 ±0.8597 V 两组对照）；
3. 零噪声校准 ideal CDAC 复测 7 个 target；
4. 全量测试套件（39 项）。

核查脚本：`reports/review_monitor_check.py`（20.3 s 可复现）。

---

## 一、总体判定

| 项 | 判定 |
| --- | --- |
| 根因结论 MODELING_BUG（decoder 归一化） | **成立，独立确认** |
| "0/4096 缺码 + 73.6 dB" | **数据属实，独立复现（73.58 dB / 0 缺码）** |
| 校准 terminal 修复（38 号 P0-A） | **修复确认，P0-A 关闭** |
| 38 号 P0-C "67.5 dB 根因" | **关闭——以构建方结论为准；本审查方同时撤回自身一处错误结论（§三）** |
| 证据包质量 | **三处缺陷 + 两处口径问题，需修复（§四）** |
| 38 号 P0-B（4/7 场景负增益） | **待重审**——v2.0.0 运行报告全部性能数字已作废，必须在新 decoder 下重生成（§五 R1） |

---

## 二、独立确认成立的部分

### 2.1 根因链条（完全确认）

老 decoder 公式 `code = signed_sum + 2047.5`（无 ÷2）是单一根因，它衍生出 v2.0.0 报告的全部"67.5 dB 谜题"：

1. signed_sum 物理范围为 ±4095 Q0（真实差分满量程 ±0.8597 V）；
2. 老 decoder 只把 ±2047.5 Q0 映射到 [0, 4095]，**信号范围上半部分被裁剪**，"测得 VFS = 0.4301 V"恰为真实满量程的一半（2047.5 Q0 × 0.21 mV/Q0 = 0.4301 V，数字严丝合缝）；
3. signed_sum 恒为奇数（3 个奇数权重 65/65/1），老 decoder 经 ties-to-even 舍入后码距 2 LSB → 恰好损失 6.02 dB → "73.5 − 6.0 = 67.5 dB"；
4. 正确的归一化是 **1 code-LSB = 2 Q0**（8190 Q0 ↔ 4096 codes）：新 decoder `code = (signed_sum + 4095) / 2` 在整数权重下为精确整数映射，L1C = 2 Q0 = **1 LSB**（不是 38 号文档推测的 2 LSB），terminal = 0.5 LSB 提供舍入。

### 2.2 独立复测数据（当前代码，审查方管线）

| 实验 | 结果 | 与构建方声称对照 |
| --- | --- | --- |
| FFT（全量程正弦，−0.5 dBFS） | **SNDR 73.58 dB**，THD 94.7 dB | 声称 73.59 ✓ |
| DC 扫描 ±0.8597 V，4097 点 | **unique 4096 / missing 0 / 非单调 0** | 声称 0/4096 ✓ |
| 构建方 dc_sweep_raw.csv 独立统计 | nominal 与 physical 两列均 4096 unique / 0 缺码 / 0 非单调 | ✓ |
| 零噪声校准 ideal CDAC（16 对） | **7/7 targets 精确命中物理值**（65/65/130/260/520/1040/2080，误差 0） | 38 号 P0-A 的 ×64/65 截尾链消失 ✓ |
| 测试套件 | **39/39 通过**（含 3 项引用 38 号文档的新增测试） | ✓ |

### 2.3 校准 terminal 修复评价

在 calDAC 6 步搜索后增加一次 terminal 判定，对欠补偿残差补 ±1 Q0（`terminal_adj`）。零噪声 ideal 下全部 target 精确命中，截尾链消除。**P0-A 关闭。**

遗留小注（不阻塞）：`terminal_adj` 的判定使用求解器无噪声符号（`initial_sign`/`final_sign`），而 `terminal_cmp_result`（含噪声比较）仅写入 trace 未参与判定——含噪运行时该设计使 terminal 位恒"正确"，略优于真实硅片表现；建议后续改为使用比较器输出并靠对平均收敛，或在文档中显式声明此简化。

---

## 三、对 38 号文档的自我修正（审查方诚实记录）

38 号 P0-C 的三条子结论分别处理：

- **(i) 奇偶锁定是确定性建模伪影** —— 维持，且被构建方证据确认。
- **(ii) "半网格稀疏是架构真实属性（底部 2 LSB 步进）"** —— **撤回**。该结论基于 ±0.4301 V 扫描（老 decoder 的假满量程），输入只激励了码域中段，−6 dB 与半数缺码均为扫描范围混淆所致。真实关系是 1 code-LSB = 2 Q0、L1C = 1 LSB，模型在新 decoder 下达到完整 12-bit。这是本审查方实验设计的失误，特此更正。
- **(iii) v2.0.0 报告的机制叙述不成立** —— 维持（"桥接电荷再分布固有特性"一说不成立，最终根因是 decoder 归一化）。

38 号的 P0-A（校准截尾）、P0-B（负增益，转入重审）、P1 清单（settling 无电压残差、亚稳态无错判、采样理想、低段/桥接失配、无 DNL/INL）不受影响，继续有效。

---

## 四、证据包缺陷（必须修复，不影响核心结论）

| # | 缺陷 | 说明与要求 |
| --- | --- | --- |
| D1 | **两个 FFT CSV 字节级完全相同** | `fft_spectrum_ideal_quantizer.csv` 与 `fft_spectrum_bridge_sar.csv` diff 完全一致——"73.59 vs 73.59, gap 0.0" 的头条证据是循环论证（同一数据分析两遍）。结论本身经本审查方独立 FFT 证实（73.58），但该交付物必须重新导出真实的 bridge SAR 频谱。 |
| D2 | **CB 扫描表与头条矛盾** | CB=1.000 行 "Missing DC Codes = 3071"，与头条 "0/4096" 直接冲突；±5% CB 行也在 3071–3081。该列的统计口径（decoder、扫描范围、是否含 terminal）必须说明并与 dc_sweep 口径统一。 |
| D3 | **Evidence §2 自相矛盾** | "Bottom effective quantization step = 2 Q0 = **2 code-LSB** … 上限 ≈68.0 dB（11-bit）" 与自家 metrics 表 "DAC step / code LSB = 2.0 Q0 = **1.0 code-LSB**" 冲突；68.0 dB/11-bit 段落是旧错误理论的残留，应删除或改写。 |
| D4 | **codebook 工具归一化过期** | `research/codebook_search.py` 的 `_target_from_vin` 用 `vin/(2·VREF)+0.5`（把 ±0.86 V 输入压缩到码本中段），且其解码走 `decode_va_exact` 互补极性路径——"codebook 67.1 vs SAR 73.6 → SAR near-optimal" 的比较口径不成立，修正归一化后重测再下结论。 |
| D5 | **两套 decoder 并存** | `fixedpoint.decode_va_exact`（raw_sum − aux_center，互补极性：code_va = 4095 − code_sar）与新 SARDecoder 缩放一致但极性相反；建议在文档中显式统一极性约定，并逐行核对 VA 第 802–829 行确认真片 decoder 归一化（排除同款归一化 bug 进入硅片解读）。 |

---

## 五、遗留行动项

| # | 行动 | 阻塞关系 |
| --- | --- | --- |
| R1 | **重生成 behavioral_sar_run_report**：v2.0.0 的 §7/§10.5/§11 全部数字（VFS=0.4301、67.5 dB、缺码 2047、7 场景增益表、MC 表）均基于老 decoder，已作废。38 号 P0-B（4/7 场景负增益）必须在新 decoder + terminal 校准下重审——预期负增益结论消失或大幅改变。 | 阻塞 P0-B 关闭 |
| R2 | 修复 FFT CSV 导出（D1），报告中补充真实 bridge SAR 频谱数据。 | — |
| R3 | 统一 CB 扫描缺码列口径（D2）、改写 Evidence §2（D3）。 | — |
| R4 | codebook 工具归一化修正与重测（D4）。 | — |
| R5 | VA decoder 与新 SARDecoder 逐行对账（D5），输出结论一句：真片归一化是否含 ÷2 映射。 | 建议优先，涉及硅片解读 |

---

## 六、监控状态板

| 审查项 | 状态 |
| --- | --- |
| 38 P0-A 校准截尾 | **关闭**（terminal 修复，7/7 精确） |
| 38 P0-C 67.5 dB 根因 | **关闭**（MODELING_BUG，独立确认） |
| 38 P0-B 负增益 | **待重审**（等 R1） |
| 38 P1 清单（settling 电压残差 / 亚稳态错判 / 采样理想 / 低段与桥接失配 / DNL-INL / PVT） | 未动，持续有效 |
| 证据包 D1–D5 | 待修复 |
| 测试 | 39/39 通过 |

**下一步：** 审查方出具 40 号《完整复现方案》（基于修正后架构：新 decoder 归一化、terminal 校准步、1 code-LSB = 2 Q0 口径），作为后续复现与交接的架构基准。
