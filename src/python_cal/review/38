# 审查结论 38：v2.0.0 运行报告终审 —— 校准可信度与 67.5 dB 根因分类

**审查对象：** `reports/behavioral_sar_run_report.md` v2.0.0（commit 1f913b8f，2026-07-23T10:41Z，声明 "READY FOR REVIEW"）

**审查链位置：** 32 执行指令 → 33 修正指令 → 34 交付要求 → 35 一审 FAIL → 36 二审 No-Go → 37 根因调查令 → **38 本文档**（后续：39 完整复现方案，由审查方出具）

**审查方式：** 独立通读全部 35 个源文件与 6 个测试文件；独立编写并执行验证实验（附件 `reports/review38_experiments.py`，只 import、不修改任何模型源码，运行 19.5 s）。本文全部数字均可由该脚本复现。

---

## 总体判定

| 项 | 判定 | 章节 |
| --- | --- | --- |
| 35/36 所提 P0 问题 | **已修复** | §1 |
| 异步 + 校准的架构/算法级复现 | **成立** | §2 |
| 37 号调查令交付合规性 | **不合规** | §3 |
| "校准有效"声明 | **No-Go**（P0-A / P0-B） | §4–§5 |
| "67.5 dB 为桥接架构固有特性"声明 | **不予接受；实验分类 = MIXED** | §6 |

一句话：模型已经可以相信"真的在跑一个异步校准 SAR ADC"，但还不能相信"校准有效"和"67.5 dB 是物理上限"这两个结论。

---

## 一、35/36 所提问题的处置确认（通过）

逐项核对 v2.0.0 报告，35/36 的阻塞问题确已修复：

1. **比较器极性**：§3 转换 trace 现同时给出 VDIFF_trial 与 VDIFF_commit，全部 14 级 CMP 与 VDIFF_trial 符号一致（1 ⇔ VTOP_P > VTOP_N）。35 §主要原因1 关闭。
2. **DC 映射语义**：§10.5 明确 VFS=0.4301 V（单边），code(0)=2048，code(±VFS)=0/4095，非单调 0。35 §主要原因2 关闭。
3. **FFT 分析器**：§11 理想 12-bit 量化器交叉验证 73.59 dB（理论 ~73.5），分析器本身可信。37 §八 前置条件满足。
4. **nominal vs physical（ideal）**：diff = 0.000 dB。37 §六 前置条件满足。
5. **事件时间线**：73 个事件单调递增，逐级满足 t_trial ≤ t_settled ≤ t_req < t_done ≤ t_commit。36 时序问题关闭。
6. **解码隔离**：257 个输入点校准前后 decisions / switch-state / comparator / timing 零失配。36 对 decoder 隔离的要求满足。

---

## 二、确认：架构/算法级复现真实成立

与 35/36 时期不同，本次不再怀疑"模型是否在真跑"。依据：

- **物理层**：全差分桥接 CDAC（低段 6 + CB=2Cu + 高段 7，Cu=4 fF），底板采样，VTOP/VBRIDGE × P/N 四浮空节点 2×2 电荷守恒精确求解，守恒误差 ~4×10⁻²⁹ C（浮点噪声级）；单电容 P/N 对称性与 complementary=2×P-only 验证通过。
- **异步层**：事件驱动（trial→settled→cmp_request→cmp_done→commit），比较器 t0+τ·ln(Vlogic/|Vdiff|) 再生模型 + 亚稳态检测 + max_wait；实测 stage latency 2.29–6.24 ns 随输入变化、加噪声后转换时间 std 由 0.95 ns 增至 1.32 ns——逐 bit 变延迟的异步指纹真实存在。
- **校准层**：前台 7-target 升序校准，wall 由已校准电容构建，D+/D- 双向激励（结构上抵消比较器 offset），calDAC 用低段 6 个物理电容二分搜索，Q6 定点寄存器，±20% 容差；**校准控制器复用与正常转换完全相同的 CDAC 求解器与比较器请求路径**，physical weights oracle 不进控制逻辑。
- **验证体系**：36 单元测试全过；三 decoder（nominal/calibrated/physical）同批 decisions 归因法正确。

---

## 三、对 37 号调查令的合规性检查：不合规

37 号文档 §十 要求的交付物逐项核对：

| 37 号要求 | 状态 |
| --- | --- |
| bridge_root_cause_report.md / .json | **未提交** |
| dc_sweep_raw.csv / reachable_levels.csv / single_cap_steps.csv / conversion_trace_*.csv / fft_spectrum_*.csv | **未提交** |
| §九 A–E 区分实验（等效低段替换、exhaustive 对照、三 decoder 对照、H1C-A=64 敏感度、CB ±5% 扫描） | **未执行** |
| 最终分类 ARCHITECTURE LIMIT / MODELING BUG / MIXED / INCONCLUSIVE | **未给出** |

已满足的前置项（给予肯定）：理想量化器 73.59 dB（§八前置）、ideal nominal==physical 0.000 dB（§六前置）、缺码数 2047 与零非单调（§三部分）。但 §三 明确要求的**缺失码列表及奇偶模式**未提供——本审查 §6 已补做该项。

**结论：v2.0.0 报告 §11 注中"这是桥接 CDAC 架构的已知特性"一句，按 37 号文档自身判据（"在这些文件和数据提交前……只能视为假设"），当前状态 = 未证实假设，必须降级或补证。**

---

## 四、P0-A：主校准流程存在系统性截尾偏差

**证据（报告 §7，AVG_PAIRS=16、noise=0、ideal CDAC）：** 7 个 target 测得值全部恰好等于物理真值 × 64/65：

| Target | 物理真值 (Q0) | 测得 (Q0) | 误差 |
| --- | --- | --- | --- |
| H1C-A | 65 | 64 | 1.54% |
| H1C-R | 65 | 64 | 1.54% |
| H2C | 130 | 128 | 1.54% |
| H4C | 260 | 256 | 1.54% |
| H8C | 520 | 512 | 1.54% |
| H16C | 1040 | 1024 | 1.54% |
| H32C | 2080 | 2048 | 1.54% |

**机理（已从源码定位）：** calDAC 搜索只有 6 个偶数权重位（64,32,16,8,4,2），最大码 126，无 rounding/terminal 位。零噪声下搜索确定性截尾：target 0（H1C-A，真值 65）被截到 64；此后每个 target 的 target_phys − wall_phys = 0（ideal CDAC），残差测量为 0，测得值 = wall 校准和——偏差沿 wall 链 1:1 传递，形成全链 ×64/65。H32C 权重偏差 32 Q0 ≈ 0.78%，恰在 ±20% 容差内通过验收，但足以污染所有后续 SNDR 归因。

**反证（同一报告内）：** §8 改用 σ=0.01 V 抖动后，H4C 测得 258.56（真值 260，误差 0.55%），显著好于零噪声的 256（1.54%）。这证明 VA 原设计依赖"抖动 + 多对平均"获得亚 LSB 分辨率，而 v2.0.0 的主校准流程（§7 及 §11 全部场景）使用零噪声 + 16 对，绕开了该机制。

**附带问题（必须一并处理）：** 校准噪声配置存在三处互相矛盾的来源——`config.CAL_NOISE_SIGMA = 0.50 LSB`（策略文档最优抖动）、`AsyncCalibrationController.cal_noise_sigma = 0.01 V`（≈48 Q0，与注释"低至 ~0.15 Q0"明显不符）、报告实际运行 `noise = 0`。`AVG_PAIRS` 同样存在 config=128 / 报告=16 的不一致。

---

## 五、P0-B：7 个失配场景中 4 个校准后 SNDR 反而变差

| 场景 | Nominal SNDR | Calibrated SNDR | Cal gain |
| --- | --- | --- | --- |
| ideal | 67.5 | 64.8 | **−2.8** |
| common −2% | 63.3 | 61.2 | **−2.1** |
| P/N asym | 67.5 | 64.8 | **−2.7** |
| bridge +2% | 63.7 | 61.0 | **−2.7** |
| common +2% | 63.3 | 66.1 | +2.8 |
| alt ±2% | 34.9 | 58.8 | +23.9 |
| H32C +5% | 35.5 | 63.5 | +28.0 |

**因果链：** P0-A 使高段权重统一 ×64/65，而低段（calDAC 参与正常转换）保持标称 → H1C-R/A 与 L32C 段间权重不连续 ±1 Q0 → 段边界系统性 DNL → 在中小失配场景，校准引入的权重误差大于失配本身。最终判定条件"至少一个场景改善"被大失配场景（+28.0 / +23.9 dB）满足而放行，掩盖了普遍场景下的负增益。

**通过判据（修复后必须满足）：** 重出 7 场景表，ideal 场景 |cal gain| ≤ 0.5 dB，无任何场景 cal gain < −1 dB，大失配场景保持显著正增益。

---

## 六、P0-C：67.5 dB 根因 —— 独立实验结果与分类

### 6.1 审查方独立实验（附件脚本，未修改任何模型源码）

| 实验 | 配置 | 结果 |
| --- | --- | --- |
| A | ideal CDAC + nominal 解码，FFT 4096/K=127/−0.5 dBFS | SNDR **67.52 dB**，THD 87.9 dB（与报告 §11 交叉一致） |
| B | ideal CDAC + 穷举排序码本最近电平搜索（research/codebook_search，绕过贪心 SAR） | SNDR **61.07 dB** |
| C | 高段电容 ±0.1% 非比例扰动（caps_override）+ physical 解码 | SNDR **66.68 dB**；物理权重变为非整数（2080.700 / 1038.686 / …） |
| D1 | ideal，4097 点 DC 扫描 | unique 2049，missing **2047**，非单调 0；**缺失码 2047 个全为偶数**（奇偶锁定） |
| D2 | 扰动 CDAC，4097 点 DC 扫描 | unique 2052，missing 2044，非单调 0；缺失码奇偶 **1022/1022**（锁定瓦解） |

### 6.2 三个子结论

**(i) 奇偶锁定是确定性建模伪影。** ideal 下产出码全部落在单一奇偶性（缺失码全为偶数）；±0.1% 扰动后奇偶模式立刻瓦解为近似均匀分布（1022/1022）。完美奇偶锁定只可能来自"精确整数权重 + 零噪声 + 定点舍入"的组合，真实硅片不存在。这正面回答了 37 号 §三"缺失码是偶数、奇数还是其他模式"的未答问题。

**(ii) 半网格稀疏与 −6 dB 是架构真实属性（在当前权重集合 + 解码归一化下）。** 扰动打破整数网格后，缺失码仍 2044、SNDR 仍 66.68；穷举码本搜索（B=61.07）同样未恢复——瓶颈不在贪心 SAR 搜索，而在 DAC 电平集合本身。物理根源明确：**最细物理步进 L1C = 2 Q0，terminal（1 Q0）无物理电容，而 Q0:LSB = 1:1（4095 Q0 ↔ 4095 codes）**——底部有效量化步进为 2 LSB，恰好损失 6.02 dB（73.5 → 67.5），与观测严丝合缝。

**(iii) 报告的机制叙述不成立。** "桥接电容电荷再分布导致部分码点 ±1 LSB 偏移、noise floor 升高"无法解释完美奇偶锁定，也无法解释为何 exhaustive 搜索更差。正确叙述是底部量化步进加倍，而非随机扰动。

### 6.3 分类（按 37 号 §十 格式）

```text
结论：MIXED
证据：
  (i) 奇偶锁定 = MODELING 类伪影（exact-integer + noise-free 特有；D1 vs D2 证实）；
  (ii) 半网格稀疏 = ARCHITECTURE 类真实属性（C/D2/B 证实，扰动与穷举均不消除），
       根源 = 最细物理步进 2 Q0 + terminal 无物理电容 + Q0:LSB=1:1；
反证实验：本审查 A/B/C/D1/D2（reports/review38_experiments.py，19.5 s 可复现）；
仍未排除的问题：
  1. 真实工作点（比较器噪声 σ≈0.15 LSB）下噪声抖动是否填满码网格——
     若填满，则 noise=0 的"固有限"叙述对真片不成立，必须改写；
  2. 37 §九 D/E（H1C-A 65→64 敏感度；CB ×{0.95,0.975,1.0,1.025,1.05} 扫描）未做，
     "稀疏不可避免"未证明；
  3. THD 口径：报告 −97 dB vs 本审查 h2..32 全积分 87.9 dB，须统一谐波计入范围。
```

**对报告 §11 注的处置要求：** 在以上三项补齐前，"桥接 CDAC 固有 SNDR ~67.5 dB"必须改写为："确定性无噪声工作点下的有效量化步进为 2 LSB（L1C=2 Q0、terminal 无物理电容），SNDR 上限 67.5 dB；真实含噪工作点的上限待补测"。

---

## 七、P1 清单（不阻塞架构结论，阻塞电路级外推）

1. **settling 只有时间、没有电压残差**：`ideal_settling=False` 仅加 5τ 延迟，`solve_current()` 永远返回终值；不完全建立误差不进判决。`dac_residual_target_v` 参数定义后从未使用。
2. **亚稳态不产生错判**：metastable 时决策时间拉满 max_wait，但输出仍按 vdiff_eff 符号——真实超时强制判决的错代价未建模。
3. **采样完全理想**：无 kT/C 噪声、无 jitter、无电荷注入/时钟馈通，顶板理想钳位 VCM。
4. **calDAC 码值用标称低段权重编码**：低段失配不进校准闭环；桥接电容失配不可校且未进 MC。
5. **无 DNL/INL 扫描、无 PVT**（报告 §15 已自述，符合诚实性要求；但 §4 主校准零噪声配置未列入已知问题，必须补声明）。

---

## 八、复审提交要求（量化判据）

1. **修 P0-A**（二选一）：(a) calDAC 搜索加 rounding/terminal 判定位（模型增强，须显式标注与 VA 的差异）；或 (b) 报告默认校准配置改为抖动噪声 + AVG_PAIRS=128（对齐 VA 原设计），零噪声模式仅作 debug 并在报告声明。同时统一三处校准噪声配置来源并给出取值论证。
2. **P0-B 回归**：重出 7 场景校准增益表，满足 §五判据（ideal |gain| ≤ 0.5 dB；无场景 < −1 dB；大失配保持正增益）。
3. **§6.3 三项补做**：含噪工作点 DC/FFT；H1C-A=64 敏感度；CB ±5% 扫描。提交原始 CSV（沿用 37 §三/§四/§八 格式）并重新分类。
4. **报告 §11 注释改写**（按 §6.3 要求文本）。
5. **THD 口径统一**后重报 §11 全部 THD 数字。
6. **diff 说明**（相对 commit 1f913b8f）+ 逐条回复本文档 §4–§7。
7. 复现与修复的架构基准以 **39 号《完整复现方案》** 为准。

---

## 九、监控与交接条款

- 本审查方将核对每一次重新提交对上述判据的满足度；**只交结论不交原始数据视为未交**。
- 若执行方两次提交仍不满足判据，或声明无法完成，必须输出交接文档（编号顺延至下一可用号），内容至少包括：当前 commit 与测试状态；每个 P0/P1 的分析、已尝试方案及结果；源码文件级地图与不变量（decoder 隔离、physical oracle 不得进控制逻辑、比较器极性、Q0 归一化口径）；每条实验的精确复现命令；剩余假设清单。交接后由本审查方接管执行，接管基准为 39 号方案。

---

## 附件

- `reports/review38_experiments.py`：审查方独立实验脚本（A/B/C/D1/D2）。
- 原始输出：

```json
{
 "A_ideal_nominal_FFT": {"sndr": 67.52, "thd": 87.94},
 "B_ideal_codebook_FFT": {"sndr": 61.07, "thd": 79.43},
 "C_perturbed_physical_FFT": {"sndr": 66.68, "thd": 74.83,
   "phys_w_stage0to6": [2080.7, 1038.686, 520.695, 259.723, 130.148, 64.957, 65.048]},
 "D1_dc_ideal_nominal": {"unique": 2049, "missing": 2047, "nonmono": 0,
   "missing_even": 2047, "missing_odd": 0},
 "D2_dc_perturbed_physical": {"unique": 2052, "missing": 2044, "nonmono": 0,
   "missing_even": 1022, "missing_odd": 1022},
 "elapsed_s": 19.5
}
```

- 环境：Python 3.13.12 / NumPy 2.4.2 / Windows。交叉核对：A 与 D1 结果和 v2.0.0 报告 §10.5/§11 完全一致（67.5 dB、missing 2047、非单调 0），证明本审查管线与报告管线等价可比。
