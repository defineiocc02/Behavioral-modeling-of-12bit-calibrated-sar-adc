# 审查结论 42：C1 验收（部分通过）+ P/N asym 残余根因裁定 + C4a 强制扩展与任务排序指令

**审查链：** … → 41 R1 验收与文献对标 → **42 本文档**（《完整复现方案》顺延 43）
**输入：** 构建方 C1（terminal 门限化）、C4（caldac_ruler_error.py 有界性证明框架）、R5（r5_va_decoder_audit.py）交付与重生成报告（21:16）；构建方询问"R3/R2/R7/R6/C5 全部推进还是选几项"。
**本审查方新增独立实验（三组，均可复现）：**
- E1：当前代码零噪声 P/N asym 校准抽查（`reports/review41_pn_check.py`）
- E2：0.5 LSB rms 抖动 + 128 对的 P/N asym 校准（生产 API，未改任何文件）
- E3：terminal 门限 1 Q0 → 0.5 Q0 的运行时猴子补丁对照（仅审查方进程内，未改任何文件）

---

## 一、C1 验收意见：部分通过

| 判据（41 号 C1） | 结果 | 判定 |
| --- | --- | --- |
| +1/靶同向偏差消除 | dplus/dminus 由 +1/+1 变为 0/0（小残差靶） | ✅ 门限化方向正确 |
| P/N asym 场景 cal gain ≥ −0.5 dB | **−3.7 dB**（−5.4 改善，未达标） | ❌ |
| P/N asym 逐 target = 物理 ±0.5 Q0 | H1C-A −0.53，H32C −15.9（×129/130 链） | ❌ |
| ideal 7/7 精确不退化 | 保持 | ✅ |

**结论：C1 消除了符号-only 偏差（主要疗效），但残余偏差另有病根，不属于 terminal 门限问题（§二实锤）。C1 判为"通过（作为必要非充分修复）"，P/N asym 场景仍不通过。**

## 二、残余偏差根因裁定：与 bridge +2% 同根（Chen §8.2 尺子误差）

**三组实验排除法：**

| 实验 | 配置 | H1C-A 测得 | 结论 |
| --- | --- | --- | --- |
| E1 | 零噪声 | 64.5（d+63 / d−66） | ×129/130 链 → H32C −15 |
| E2 | 0.5 LSB 抖动 + 128 对 | 64.47（±0.06 统计窗内一致） | **不是量化噪声问题，C2 抖动救不了它** |
| E3 | 门限 1→0.5 Q0（猴子补丁） | 64.47 纹丝不动 | **与 terminal 门限无关** |

**机理（电压实锤）：** P/N asym 下高段电容步进自归一化（分子分母同比例，实测 P/N 单边步进仅差 0.12%），**但 calDAC（低段）电容不随 md/mu 缩放，其步进只随该侧顶板总电容变化**：N 侧总电容 −2%（64.66 vs 65.94 Cu 等效）→ N 侧 calDAC 步进 **+1.98%**。于是 D+ 方向出现毫厘级穿越：

```text
D+ 残差（target P 步进）    = 13.657 mV
L32C N 侧步进（calDAC MSB）= 13.439 × 1.0198 = 13.705 mV
13.705 > 13.657 → 贪心搜索误判"加过了"，拒绝 L32C → 码 62+1 = 63（真值 ≈65）
D- 方向镜像（P 侧尺子偏轻）→ 66；平均 −0.5 Q0 → 沿 wall 链放大（×129/130）
```

**这与 bridge +2%（低段整体 +1.94% → ×63/65 链）是同一个 Chen §8.2 第一类误差源：后端尺子的物理权重偏离标称，而码值按标称换算。** 差别只是 bridge 是整段均匀偏移，P/N asym 是分侧偏移（互补平均≈标称，单边 ±2%）。

## 三、裁定：C4 必须以 option(a) 执行（calDAC 自校准扩展）

41 号给 C4 留了 (a) 扩展校准 / (b) 有界性证明两个选项。现证据明确：

- **P/N asym −3.7 dB 是测量错误，不是架构上界**（physical decoder 73.5 dB 同权重集可达）——有界性证明路线（C4b）对它不适用；
- **唯一根治路径（两场景共用一个修复）**：将 calDAC 6 位（L32C..L1C）纳入校准——用**已校准的高段作反向尺子**实测各 calDAC 位的（分侧）物理权重，码值换算从"标称低段权重"改为"实测低段权重"。
- 构建方已交付的 `calibration/caldac_ruler_error.py`（Chen §8.2 预测框架）保留为**验证资产**：C4a 修复后，预测 ε_caldac 与实测 α 的一致性须作为报告一节；不作为豁免依据。

**C4a 验收判据：**
1. P/N asym 与 bridge +2% 逐 target 测得 = 物理 ±0.5 Q0；
2. 7 场景 cal gain 全部 ≥ −0.5 dB（bridge 场景允许用"calibrated vs physical gap ≤ 0.5 dB"替代，因其残余为低段固有 INL）；
3. ideal 7/7 精确（65/65/130/260/520/1040/2080）不退化；
4. 抖动 + 128 对成为默认配置（C2 同批落地，三处噪声配置统一）。

## 四、任务排序指令（替代 41 号 §六尾段）

**不要直接全推 R3/R2/R7/R6/C5。** 正确顺序：

| 序 | 任务 | 说明 |
| --- | --- | --- |
| 1 | **C4a**（calDAC 自校准扩展） | 唯一硬阻塞；P/N asym 与 bridge 的共根修复 |
| 2 | **C2**（默认抖动 + 128 对）与全量重生成 | 与 C4a 同批，一次出报告 |
| 3 | **R6**（python_cal 入 git） | 在 C4a+C2 全绿基线上提交，别在修复半程提交 |
| 4 | **R2 / R7 / R3**（可并行） | 均不接触校准链路：R2 频谱导出修复、R7 reachable 拆分+63/65、R3 codebook 归一化+残差优势断言 |
| 5 | **C5**（验证矩阵固化）最后 | 其中 P/N asym 靶精度测试、D+/D- 对称性测试**现在就可写**（C4a 未合入时应为红测，合入后转绿）；禁止写成适配当前行为的绿测 |

**R5 交付（r5_va_decoder_audit.py）本审查方将单独验收，结果补记。** 两次提交仍不满足 C4a 判据 → 触发 38 号 §九交接条款。
