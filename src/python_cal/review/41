# 审查结论 41：R1 交付验收 + 校准算法文献对标 + 校准有效性专项要求（强制）

**审查链：** … → 38 独立审查 → 39 监控结论 → 40 合并仲裁 → **41 本文档**（《完整复现方案》顺延 42）
**子文件夹公告：** 自本文档起，审查方编号文档统一存放于 `review/` 子目录（38/39/40 已迁入）。
**输入：** 构建方 R1 交付（重生成 behavioral_sar_run_report + forensic 修复）、"要继续哪些"的询问；三份参考文献（Wang 2022《一种 SAR ADC 电容失配自测量与数字校准技术》、失配与位权自校准专业报告（综合 Omran/Bagheri/Chen/郑瑜/Huang 五项）、Huang 2024 学位论文 §4.5）。
**本审查方新增独立证据：** `reports/review41_pn_check.py`（P/N asym 与 bridge +2% 两场景的逐 target 校准测量抽查 + 三 decoder SNDR，11 s 可复现）及 target 级校准 trace 分析。

---

## 一、R1 交付验收（结论先行）

| R1 判据（40 号 §五） | 结果 | 判定 |
| --- | --- | --- |
| ideal cal gain ≥ −0.1 dB | +0.0（73.7/73.7） | ✅ |
| 7/7 target 精确（零噪声 ideal） | 65/65/130/260/520/1040/2080，误差 0 | ✅（其交付物内自证，非仅本审查方旁证） |
| DC 缺码 = 0 | 0 | ✅ |
| 无任何场景 cal gain < −1 dB | **P/N asym −5.4、bridge +2% −1.2** | ❌ 两项违规 |
| 大失配保持正增益 | H32C+5% +27.5、alt±2% +29.8、MC +12.2/+8.7/+9.4 | ✅ |
| 旧报告作废标记/过时叙述清理 | 868 行等已修 | ✅ |

**R1 总体：部分通过。两个违规场景不接受"已知架构限制"的口头解释——本审查方已用逐 target 数据证伪（§二/§三）。** MC 三样本全正值得肯定。

## 二、P/N asym −5.4 dB：根因裁定（测量链 bug，非架构限制）

构建方解释："校准返回单权重，无法编码 P/N 不对称"。**驳回，三重证据：**

1. **物理 decoder（同样单权重集）在该场景 = 73.5 dB**（本审查方复测；构建方表内 73.7 一致）。单权重解码完全可以表示该场景——因为 ±2% 共模缩放自归一化（见 §四.2，文献式 3.32：δC=αC → δW=0），物理权重与标称一致（oracle 实测 65/130/…/2080 全为标称值）。
2. **逐 target 测量数据**（本审查方抽查，16 对零噪声）：cal 权重 2111/1056/528/264/132/66/65 = 物理 × **66/65**（+1.54%），H1C-A 本身测准（65.0），从 H1C-R 起每个 wall target 的 dplus=dminus=**+1**（应为大小相等、符号相反）。
3. **trace 实锤（target 1, H1C-R）**：两侧残差真值仅 ±0.08 Q0（+0.0161 mV，因自归一化），calDAC 全部 6 位正确拒绝；问题出在 **terminal 判定步**：`terminal_adj` 只看残差符号、不设幅度门限——残差 +0.08 Q0（远小于 1 Q0）也触发 +1 Q0 调整，且 D+/D- 两方向同向触发 → 平均后每 target 净 +1 Q0 偏差 → 沿 wall 链逐级放大（65→66→132→…→2111，恰为 Chen 递归传播 (I−A)⁻¹η 的实测形态）。

**附加违规点：** `terminal_adj` 用求解器无噪声符号判定，`terminal_cmp_result` 仅写入 trace 未参与逻辑——含噪运行时该 terminal 位恒"正确"，优于真实硅片，39 号已备注仍未处理。

**ideal 场景为何能过：** 残差恰为 0（sign=0）→ terminal 不触发 → 测量精确。该 bug 只在残差非零时显现，P/N asym 的微小残差恰好暴露它。

**判据：** 修复后 P/N asym 逐 target 测得值 = 物理值 ±0.5 Q0，该场景 cal gain ≥ −0.5 dB。

## 三、bridge +2% −1.2 dB：根因裁定（calDAC 尺子误差，38 号 P1#4 实锤）

构建方解释："桥接电容影响低段有效权重，校准只覆盖高段"——方向对但不完整。本审查方逐 target 数据显示**两层误差**：

1. **测量层**：CB +2% → 低段物理权重 +1.94%（65.24/32.62/…），但 calDAC 码值仍按**标称**低段权重编码 → H1C-A 测得 63（真值 65，欠读 2 Q0）→ wall 链放大为 ×63/65（2016/1008/…/63）。**这正是 Chen §8.2"后端 DAC 不是理想尺子"的第一类误差源（未校准低位电容失配）。**
2. **转换层**：低段解码权重保持标称而物理 +1.94%，形成残余 INL（physical decoder 也只剩 69.8）。

**判据（二选一）：** (a) 扩展校准覆盖低段（高段校准完成后可反作尺子）；或 (b) 正式给出有界性证明——calibrated 残差 = 低段权重误差预测值，并将验收判据修订为"仅低段/桥接失配场景允许 cal gain ≥ −1.5 dB 且附预测一致性数据"。

## 四、文献对标：算法骨架成立，四处实现选择偏离文献明确指导

对标来源：Wang 2022（误差电压自测量 + 权重/增益两步校正）、Chen 2024（递归位权校准误差分析）、Bagheri 2020（确定性+随机量化）、Huang 2024 §4.5（16 位 split-sampling 真片校准）、五项综合专业报告（§12 直接映射本类 12 位 14 判决方案）。

### 4.1 骨架与文献一致的部分（予以确认）

- bottom-up 递归 wall（已校准位加入后端）= Chen §6 / 报告 §6、§12.2 的标准结构；第一被校位满足 W_i < ΣW_j（可观测条件，报告式 5.9/6.3）✓
- D+/D- 相反极性测量消比较器 offset = Chen §6.5 数字 CDS / Huang §4.5 式 4.17（D+−D−)/2 / Wang 式(3)-(5) ✓
- 低段作 calDAC 量化残差 = Chen/Bagheri 后端 DAC 思想 / Huang "6 capacitors in the LSB section as the calibration DAC" ✓
- Q6 定点权重寄存器、数字域重构（decoder 隔离）= 报告 §6.6/§4.4 ✓
- 校准走真实物理路径（同 solver/同比较器）——高于文献常见的行为级做法，予以肯定 ✓

### 4.2 四处偏离（每条都有实测失败对应）

| # | 偏离 | 文献原文依据 | 实测后果 |
| --- | --- | --- | --- |
| D1 | **零噪声确定性搜索**（正式运行 noise=0、16 对） | Chen §8.3："纯确定性重复测量总会得到同一个码，不能通过平均消除偏差；校准噪声约高于 0.5 LSB rms 时长期平均接近真值"；Bagheri §7.1 | 38 号 P0-A ×64/65 截尾链（已被 terminal 修补，但引入 D2 新偏差） |
| D2 | **terminal 判定只判符号、无幅度门限、用无噪声符号** | Bagheri §7.2 亚 LSB 必须靠噪声统计量化（p∈(0.1,0.9) 窗口）；Huang 依赖真实含噪比较 + 32 对平均 | P/N asym ×66/65 偏差链，−5.4 dB（本文 §二） |
| D3 | **calDAC 码值用标称低段权重** | Chen §8.2 后端 DAC 三类误差源之首："未校准低位电容本身的失配" | bridge +2% ×63/65 偏差链，−1.2 dB（本文 §三） |
| D4 | **校准后无显式增益模态归一化** | Chen §8.4/8.6：传播误差"主要形成增益误差"，"校准后必须做总权重归一化/冗余偏移重算，否则增益项保留" | ×66/65 的增益分量未在主流程显式消除（decoder 仅部分归一化） |

### 4.3 场景本身评判

"P/N asym"（全高段 P+2%/N−2%）在物理上接近**良性场景**：报告式 3.31-3.32（δC_k=αC_k0 → δW_k=0）与该场景 oracle（物理权重=标称）一致。它作为校准链压力测试很有价值（暴露了 D2），但**任何"该场景性能受限是架构原因"的说法都不成立**。

### 4.4 两种算法世系与递归传播的必然性

- **Huang 真片世系**：无 wall，每个高位直接用 calDAC 独立测量（高位保持 Vcm；calDAC 量程按 1 bit + 对称冗余设计）→ 无递归传播，代价是 calDAC 量程需求大。
- **Chen/模型世系**：bottom-up 递归 wall → calDAC 只需覆盖残差，但 η 沿 (I−A)⁻¹ 传播（报告 §12.5.2：e0=2η2+η1+η0，最早被校位误差以系数 2 进入 MSB）。
- 对本权重集合（calDAC 126+1 vs target 2080），wall 不可避免 → **D1/D2/D4 的缓解措施是强制的，不是可选的**。报告 §12.5.2 另给出两条资源策略：较早基准位用更多平均次数；或对最终权重做联合最小二乘/归一化。

### 4.5 与 Wang 2022 的两点对照

- Wang 用"位 vs 全部低位之和"(011…1↔100…0) 免递归，随后做权重校正 + 低位增益校正两步（式 7-8）；模型选择 Chen 递归 wall 并用 decoder 归一化替代增益步——可接受，但 D4 要求把归一化显式化。
- Wang 明确"LSB 段失配影响较小、无需单独测量校准"是有前提的：LSB 段不作尺子用。本模型 calDAC 兼任尺子，D3 不适用该豁免。

## 五、校准有效性专项要求（强制，编号 C1–C5）

| # | 要求 | 验收判据 |
| --- | --- | --- |
| C1 | **terminal/rounding 修复**：二选一——(a) terminal 调整加幅度门限（仅当同号且残差 ≥ 1 Q0 才 ±1）；(b) terminal 位改用含噪比较器输出 + 对平均（Bagheri/Huang 路线）。同时处理 39 号备注：`terminal_adj` 不得使用无噪声求解器符号。 | P/N asym 逐 target 测得 = 物理 ±0.5 Q0；ideal 保持 7/7 精确；7 场景 cal gain 全部 ≥ −0.5 dB |
| C2 | **默认校准配置改为抖动+足够平均**：cal_noise ≈ 0.5 LSB rms（用 compute_noise_sigma_v_from_lsb 换算，config.CAL_NOISE_SIGMA 语义来源：Chen §8.3），AVG_PAIRS=128（Huang 32-64 对量级）；零噪声仅作 debug 并在报告声明 | 报告 §7 配置块与 config/控制器默认值三处一致 |
| C3 | **增益模态处理显式化**：校准完成后计算并报告总权重与冗余中心偏移（报告式 12.26 动态 O_Q），给出校准前后 decoder 归一化分母；证明残余误差中增益模态已被 decoder 归一化吸收（Chen §6.4/8.4 模态分解一段说明） | 报告新增一节，含 e=αW0+e⊥ 分解数字 |
| C4 | **calDAC 尺子误差处置**（本文 §三判据 a/b 二选一） | (a) 低段扩展校准后 bridge+2% cal gain ≥ −0.5 dB；或 (b) 有界性证明 + 判据修订文本 |
| C5 | **校准专项验证矩阵固化**（报告 §13.2 映射为永久测试）：D+/D- 对称性检查（每 target 码和/码差）；权重注入实验两例（P/N asym 与 bridge +2%，即本审查方 review41_pn_check 的等价物，分离"被校位误差"与"后端尺子误差"）；AVG_PAIRS 扫描（锁码区间检查）；MC 必须先校准再转换 | 进入 tests/ 并全绿 |

## 六、对"要继续哪些"的排序答复

1. **C1（最高优先）**——校准有效性的最后阻塞项；不修则 P/N asym 的 −5.4 dB 永远在。
2. **R5（VA decoder 逐行对账）**——涉真片解读，decode_va_exact 与新 SARDecoder 互补极性（code_va = 4095 − code_sar）写入文档。
3. **C2/C3**——与 C1 同批完成，一次重生成报告覆盖。
4. **C4**——判据 a 工作量大则先交 b（有界性证明）。
5. **R3**（codebook 归一化 + 残差优势断言）、**R2**（FFT CSV 独立重导）、**R7**（reachable 拆分 + 63/65 更正）、**R6**（python_cal 入 git）、**C5**（测试固化，随 C1 一起）。

**两次提交仍不满足 C1 判据 → 触发 38 号 §九交接条款。**

## 附件

- `reports/review41_pn_check.py`：两违规场景逐 target 校准测量 + 三 decoder SNDR 抽查（输出 JSON 见本审查方记录，关键数字已引用于 §二/§三）。
- target 1（H1C-R）校准 trace：两方向初始残差均 +0.0161 mV，6 位全拒，terminal 步两方向同向 +1 —— D2 根因的直接影像。
