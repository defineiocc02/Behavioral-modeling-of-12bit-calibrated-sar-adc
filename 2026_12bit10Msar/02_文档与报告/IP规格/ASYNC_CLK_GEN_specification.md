---
title: ASYNC_CLK_GEN 模块 IP 规格说明书
aliases:
  - 异步时钟发生器
  - Gated Ring Oscillator
  - ASYNC_CLK_GEN
tags:
  - 数字逻辑
  - IP规格
  - 异步时钟
created: 2026-03-23
updated: 2026-03-24
---

# ASYNC_CLK_GEN 模块 IP 规格说明书

本模块为 12-bit 50-MS/s SAR ADC 专用异步时钟发生器（ASYNC_CLK_GEN），采用**门控自由环形振荡器（Gated Free-running Ring Oscillator）**架构，集成了启动脉冲注入器、奇数级环形振荡核心、输出缓冲树和休眠钳位逻辑。

---

## 1. 模块接口定义

### 1.1 端口定义

| 端口名 | 方向 | 位宽 | 类型 | 功能描述 |
|:------|:-----|:-----|:-----|:---------|
| CLK00 | input | 1 | wire | 全局启动信号（ADC 采样结束，转入转换阶段时拉高） |
| SET_12 | input | 1 | wire | 转换完成标志位（SET<12>，第 12 位比较完成后拉高） |
| DVDD | inout | 1 | wire | 数字电源（1.8V） |
| DGND | inout | 1 | wire | 数字地 |
| PCLK | output | 1 | wire | 内部核心振荡时钟（可供测试观测） |
| CCLK | output | 1 | wire | 比较器触发时钟（需强驱动） |
| CLK | output | 1 | wire | DAC/寄存器逻辑时钟（需强驱动） |

### 1.2 Verilog 原型

```verilog
module ASYNC_CLK_GEN (
    input  wire CLK00,
    input  wire SET_12,
    inout  wire DVDD,
    inout  wire DGND,
    output wire PCLK,
    output wire CCLK,
    output wire CLK
);
endmodule
```

---

## 2. 内部子模块架构

本模块由 4 个子模块构成：

1. **第一级：启停总控逻辑 (Start/Stop Gating Logic)**
2. **第二级：启动脉冲注入器 (Startup Pulse Injector)**
3. **第三级：自激振荡主环路 (Ring Oscillator Core)**
4. **第四级：输出缓冲树 (Output Buffer Tree)**

---

## 3. 子模块详细剖析

### 3.1 第一级：启停总控逻辑

**涉及元件：** I32(INV), I33(NAND), I58(INV)

**逻辑表达式：**
- 输入端 SET<12> 首先经过 I32 取反生成 net9
- net9 与全局启动信号 CLK00 进入 I33(NAND)，输出内部使能 EN（Active-Low 低有效）
- EN 经过 I58 取反，生成真正的高电平有效使能信号 net14

**真值表：**

| SET_12 | CLK00 | net14 |
|:-------|:------|:------|
| 0 | 0 | 0 |
| 0 | 1 | **1（允许振荡）** |
| 1 | 0 | 0 |
| 1 | 1 | 0 |

**工作状态：**
- 当 SET<12> = 0（正在转换）且 CLK00 = 1（启动）时 → net14 = 1（允许振荡）
- 当 SET<12> = 1（转换结束）时 → 不管 CLK00 是什么，net14 = 0（强制关断全系统）

---

### 3.2 第二级：启动脉冲注入器

**涉及元件：** delay_450p, I42(NAND)

**功能：** 这是一个非常经典的 Edge-to-Pulse（边沿转脉冲）发生器。

**逻辑流向：**
- 信号 net14 分成两路：一路直接进入 I42(NAND)；另一路穿过包含 13 级反相器的 delay_450p，生成延迟且反相的信号 net22，再进入 I42(NAND)

**工作原理：**
1. 起振瞬间：当 net14 突然从 0 变 1 时，由于延迟，net22 在最初的 450ps 内依然保持为 1
2. 此时 NAND 门的两个输入全为 1，输出 net23 会瞬间被拉低
3. 450ps 后，net22 变为 0，net23 恢复为 1

**模块功能：** 在电路启动瞬间，在 net23 节点上"人为制造"了一个宽度为 450ps 的低电平触发脉冲，用来打破环路的死锁状态，强制环形振荡器起振。

---

### 3.3 第三级：自激振荡主环路

**涉及元件：** I43(NAND), I59(NAND), M10/M11(INV), delay_900p

**逻辑流向（脉冲注入后）：**

```
A → (I43) → net16 → (I59) → net12 → (M10/M11) → PCLK → (delay_900p) → 回到 A
```

因为 net23=1 且 net14=1，两个 NAND 门 I43 和 I59 在此阶段**等效为反相器**。

**致命 Bug 分析：**

> 任何环形振荡器（Ring Oscillator）要起振，**环路中的反相总级数必须是奇数 (Odd)**！

**级数统计：**

| 元件 | 反相级数 |
|:-----|:---------|
| I43(NAND) | 1 |
| I59(NAND) | 1 |
| M10/11(INV) | 1 |
| delay_900p | **27** |
| **总计** | **30（偶数）** |

**后果：** 当 450ps 的触发脉冲结束后，电路**会瞬间锁死（Latch-up）**在一个固定电平，无法振荡。

---

### 3.4 第四级：输出缓冲树

**涉及元件：** INVX4 到 INVX7 等一系列反相器

**逻辑流向：**

```
PCLK → I50 → I51 → I52 → I35 → I36 → I64 → I65 → I34 → CCLK
```

**问题诊断：** 用了 8 级反相器，并且中间包含了连续 4 个巨大的 INVX6。这属于**严重的功耗浪费和无效延时**。

---

## 4. 修复与优化指南

### 4.1 修复致命死锁 Bug

> 在 delay_900p 的原理图里，**随意删除 1 个反相器或者增加 1 个反相器**，使其内部反相级数变为 26 级或 28 级。这样总环路级数就会变成 29 级或 31 级（奇数），电路就能立刻振荡起来。

### 4.2 重构庞大耗电的 Delay 单元

**推荐结构（电流饥饿型）：**
- 用 1 个固定偏置的 **Current-Starved Inverter**（限制充放电电流）
- 配合 1 个常规 Inverter

**替换逻辑：** 只要保证替换后的 delay 单元反相极性正确，你就能用仅仅 4~6 个管子取代原本的几十个管子，**功耗瞬间暴降 90%**。

### 4.3 削减冗余的 Buffer 树

**推荐结构：** INVX1 → INVX3 → INVX8

**优势：** 这不仅缩短了时钟沿的传输延时，还省下了大笔动态功耗。

### 4.4 休眠钳位逻辑（无需修改）

当 SET<12> = 1 触发时：
1. net14 变为 0
2. I59(NAND) 的一个输入为 0，输出 net12 被强制钳位在 1（高电平）
3. PCLK 被强制钳位在 0（低电平）
4. 信号不会再往后传，环路彻底静默

**评价：** 这部分关断逻辑设计得非常完美，无需修改！

---

## 5. 优化后架构建议

### 5.1 关键参数目标

| 参数 | 优化前 | 优化后目标 |
|:-----|:-------|:-----------|
| 环路反相级数 | 30（偶数，死锁） | 29 或 31（奇数，振荡） |
| delay 单元晶体管数 | >50 | 4~6 |
| Buffer 级数 | 8 | 3 |
| 振荡器功耗 | 高（几十mW） | 低（<1mW） |
| 振荡频率 | 无法振荡 | 200MHz ~ 500MHz |

---

## 6. 完整更改方案与门级重构行动指南

针对架构缺陷与门级尺寸问题，请严格按照以下 4 个步骤在 Virtuoso 原理图中进行电路级重构：

### 步骤一：打破死锁，重构核心延时单元

**操作指令：**
1. 彻底删除原网表中的 `delay_900p`（27级反相器）和 `delay_450p`（13级反相器）子电路
2. 重新搭建一个**电流饥饿型延时单元 (Current-Starved Delay Cell)**
   - 顶部 PMOS 和底部 NMOS 接偏置电压（或电流镜），限制中间反相器的充放电电流
   - 内部节点可选择性挂载极小的 MIM 电容（如 20fF）以稳定延时
3. 在电流饥饿反相器之后，**必须紧跟一个施密特触发器 (Schmitt Trigger)**
   - 利用迟滞特性，将缓慢的充放电斜坡瞬间整形为极速翻转的方波
4. **极性核对：** 确保新的 `delay_900p` 模块输入到输出的**反相次数为奇数（1次或3次）**

---

### 步骤二：内部门级尺寸极小化

**操作指令：**
1. 打开 `NAND1` 子电路原理图
2. 将 PMOS 尺寸从 $W=900\,\text{nm}, M=4$（总宽 $3.6\,\mu\text{m}$）缩小为 **$W=1\,\mu\text{m}, M=1$**
3. 将 NMOS 尺寸从 $W=1\,\mu\text{m}, M=4$（总宽 $4\,\mu\text{m}$）缩小为 **$W=0.5\,\mu\text{m}, M=1$**
4. 将环路中用于控制和倒相的普通 `INVX2/INVX3` 全部替换为工艺库中最小驱动能力的 `INVX1`

---

### 步骤三：遵循 FO4 重剪缓冲树

**操作指令：**
1. 定位到 `PCLK` 到 `CCLK` 的输出驱动路径
2. 删除冗余的同尺寸级联：`I36(INVX6)`, `I64(INVX6)`, `I65(INVX6)`
3. 按照**逻辑努力 (Logical Effort)** 原则重构链路：修改为 3 级递增结构 `INVX1 → INVX3 → INVX9`

---

### 步骤四：脉冲宽度匹配调优

- `delay_450p` 决定了启动脉冲 `net23` 的宽度
- 在改用新延时结构后，调整其偏置，确保生成的低电平脉冲宽度在 **300ps ~ 500ps** 之间

---

## 7. 问题校准清单

在完成电路修改后，必须通过以下校准清单进行逐项 Check：

| 校验项 | 校验目标 | 校准方法 | 预期结果 | 状态 |
|:-------|:---------|:---------|:---------|:-----|
| CHK-01 | 死锁 Bug 消除 | 计算 delay_900p 输入到输出的反相极性 | 必须经过**奇数**次反相 | [ ] 待测 |
| CHK-02 | 主环路起振验证 | 跑 20ns 瞬态仿真，设定 CLK00=1, SET<12>=0，观测 PCLK | 输出连续等幅时钟方波，无锁死 | [ ] 待测 |
| CHK-03 | 启动脉冲校验 | 在起振瞬间观测 net23 节点电压 | 单次低电平脉冲，宽度 ≈400ps | [ ] 待测 |
| CHK-04 | 休眠钳位校验 | SET<12> 从 0 变 1 时观测振荡器环路 | PCLK 瞬间拉低至 0V | [ ] 待测 |
| CHK-05 | 内部短路功耗 | 观测 NAND1 门电源引脚瞬态电流 I_DVDD | 电流峰值大幅下降，无宽脉冲 | [ ] 待测 |
| CHK-06 | 系统动态功耗 | 测量转换阶段（15ns）平均消耗电流 | 功耗下降至少一个数量级 | [ ] 待测 |
| CHK-07 | 输出驱动能力 | 观测 CCLK 上升/下降沿时间，带真实比较器负载 | 边沿陡峭，上升/下降时间 <50ps | [ ] 待测 |

---

## 8. 总结

本模块的外围控制（注入脉冲 + 转换结束关断）设计堪称教科书级别。现在只需要：
1. **把笨重且偶数级的 delay_900p 换成轻量级的模拟延时单元**（修复 Bug + 降功耗）
2. **清理掉多余的 Buffer**（削减延时 + 省功耗）

即可将 ASYNC_CLK_GEN 模块完美封存，作为 ADC 设计的绝杀 IP 写入论文。