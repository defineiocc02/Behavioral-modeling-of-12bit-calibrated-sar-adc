block: sar_adc_0716_cdac_calibration
version: 1.0
process: tsmc18rf
supply: 1.8
eda_mode: full

specs:
  resolution: { target: 12, unit: bit }
  sample_rate: { target: 10e6, unit: Hz }
  coherent_fft_points: { target: 128, unit: samples }
  coherent_input_bin: { target: 59, unit: bin }
  calibration_done: { target: 1, unit: logic }
  calibration_error: { max: 0, unit: logic }
  c12_weight_residual: { max: 1.0, unit: LSB }
  normal_decode_mapping: { target: pass, unit: boolean }
  switch_physical_mapping: { target: pass, unit: boolean }
  comparator_valid_captures_per_frame: { target: 8, unit: decisions }

verification:
  corner: tt_27c
  input_half_amplitude_v: 0.8
  baseline_netlist: variants/full_adc_bypass_800m.scs
  calibrated_netlist: variants/full_adc_frame_cal_800m.scs
  note: L1/L2 deterministic verification only; PVT, Monte Carlo and post-layout are not signoff-complete.
