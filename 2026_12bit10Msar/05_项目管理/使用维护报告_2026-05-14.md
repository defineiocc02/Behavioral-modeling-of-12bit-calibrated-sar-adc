# 使用维护报告 2026-05-14

## 本次整理范围

- 将 main 工作区作为最终整理目标，并在整理前创建版本标记 `main-before-cleanup-20260514`。
- 识别 `04_辅助工具` 下 8 个工具目录为独立 Git 仓库，改为本机外部工具，不纳入当前笔记库版本管理。
- 修复 `50_归档区/PPT生成脚本` 中 PPT 生成脚本的绝对路径问题，统一输出到脚本目录的上一级。
- 删除旧 `v3` PPT 生成链条，保留 `generate_ppt_v4.js` 作为当前可维护入口。
- 清理 LaTeX/Python 生成缓存，并补充 `.gitignore` 规则。

## 使用方式

### PPT 生成

```powershell
cd "D:\ReedZhao\Document\Obsidian\日常\50_归档区\PPT生成脚本"
npm install
npm run check
npm run build
```

输出位置：

```text
D:\ReedZhao\Document\Obsidian\日常\50_归档区\Obsidian笔记库总览_25pages.pptx
```

### 外部工具

外部工具保留在：

```text
D:\ReedZhao\Document\Obsidian\日常\10_项目区\2026_12bit10Msar\04_辅助工具
```

每个工具目录单独维护 Git 状态。当前笔记库只提交说明文档与工作流，不提交嵌套仓库本体。

## 版本管理说明

- `main-before-cleanup-20260514`：main 整理前保护标记。
- `maintenance-cleanup-20260514`：前一轮在 Codex 工作树完成的维护参考标记。
- main 后续提交按用途拆分：目录重组与维护修复分开提交，便于回溯。

## 后续维护建议

- 新增生成脚本时必须使用相对路径。
- 依赖变更只改 `package.json`，不提交 `node_modules/` 与 `package-lock.json`。
- 外部工具需要固定版本时，使用正式 submodule 并提交 `.gitmodules`。
- 定期运行 `git status --short`、`git diff --check`、`node --check generate_ppt_v4.js` 做轻量巡检。
